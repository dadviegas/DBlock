{
  "name": "mblock-module",
  "version": "0.0.29",
  "description": "create modules throught mblock-webpack",
  "main": "lib/index.js",
  "module": "src/index.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "start": "babel -d lib/ src/ -w",
    "exec": "clear && npm run compile && node lib",
    "build": "npm run clean-lib && standard && npm run compile",
    "clean-lib": "rimraf ./lib",
    "install": "npm run compile"
  },
  "author": "dadviegas",
  "license": "ISC",
  "dependencies": {
    "mblock-webpack": "latest"
  }
}
