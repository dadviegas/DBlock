{
  "name": "mblock-utils-example",
  "version": "0.0.0",
  "description": "test building modules ",
  "main": "lib/index.js",
  "module": "src/index.js",
  "scripts": {
    "clean-lib": "rimraf ./lib ./es",
    "prebuild": "npm run clean-lib",
    "build": "cross-env NODE_ENV=development standard && webpack -p",
    "build:qa": "cross-env NODE_ENV=qa npm run build -- --env.NODE_ENV=qa",
    "build:production": "cross-env NODE_ENV=production npm run build -- --env.NODE_ENV=production",
    "build:es": "cross-env BABEL_ENV=es babel --no-babelrc src --out-dir es",
    "postbuild": "node lib",
    "install": "npm run build:production"
  },
  "author": "dadviegas",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^5.0.5",
    "mblock-module": "latest"
  },
  "standard": {
    "ignore": [
      "/lib/"
    ]
  }
}
