{
  "name": "mblock-consume-example",
  "version": "0.0.1",
  "description": "consume",
  "main": "index.js",
  "scripts": {
    "clean-lib": "rimraf ./lib",
    "prebuild": "npm run clean-lib",
    "build": "standard && webpack --display-error-details",
    "build:production": "cross-env NODE_ENV=production npm run build -- --env.test",
    "postbuild": "node lib"
  },
  "private": true,
  "author": "dadviegas",
  "license": "ISC",
  "dependencies": {
    "mblock-module": "latest",
    "mblock-utils-example": "latest"
  }
}
